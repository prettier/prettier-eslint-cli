import format from 'prettier-eslint'

export default formatWithOptions

function formatFilesWithOptions(argv) {
  const options = {
    
  }
}

function formatFile(filePath, options) {
  
}

function readFile() {
  
}
